flags:
  lastModified: 1h
  includePrivateRepositories: false

sourceRegistry:
  quayAPIToken: "" # token with access to read all repos

destinationRegistry:
  name: docker.io
  credentials:
    user: ""
    # base64 encoded password
    password: ""

image:
  name: giantswarm/crsync
  tag: "[[ .Version ]]"
Installation:
  V1:
    Registry:
      Domain: quay.io

metricsPort: 8000

project:
  branch: "[[ .Branch ]]"
  commit: "[[ .SHA ]]"
